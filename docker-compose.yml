version: '3.8'

# WARNING: INCOMPLETE FILE!!! DO NOT USE!!!
services:
  database:
    container_name: 'database'
    image: 'postgres:latest'
    restart: always
    ports:
      - 5432:5432
    env_file:
      - .env
